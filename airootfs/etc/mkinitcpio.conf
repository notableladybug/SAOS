#!/usr/bin/env bash
# SAOS Mkinitcpio Configuration
# Customized for Hyprland + systemd + Plymouth

MODULES=()
BINARIES=()
FILES=()
HOOKS=(base udev autodetect modconf kms keyboard keymap consolefont block encrypt filesystems fsck)
COMPRESSION="xz"
COMPRESSION_OPTIONS=(-c -T 0 --lzma2=dict=1MiB)
